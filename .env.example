CONTENT_SOURCE=local
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: HMAC secret for enhanced token security (recommended for production)
# Used to hash writeup access tokens with HMAC instead of plain SHA-256
# Generate with: openssl rand -base64 32
WRITEUP_TOKEN_SECRET=

# Email Notifications (Task 30)
RESEND_API_KEY=
EMAIL_FROM=
EMAIL_REPLY_TO=
WRITEUP_ACCESS_BASE_URL=
WRITEUP_SEND_APPROVAL_LINKS=false
